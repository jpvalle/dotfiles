# Status bar segments — source after catppuccin loads (from .tmux.conf and theme-sync.sh)
set -g status-left "#{E:@catppuccin_status_session}"
set -g status-right "#(~/.tmux/plugins/tmux-continuum/scripts/continuum_save.sh)#{?synchronize-panes,#[fg=#{@thm_red},bold] SYNC ,}#{E:@catppuccin_status_gitmux}#{E:@catppuccin_status_application}"
